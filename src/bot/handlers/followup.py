"""Обработка авто-серии follow-up сообщений после скачивания гайда.

Тексты серии загружаются из Google Sheets (лист «Авто-серия»).
Если лист недоступен, используются fallback-тексты.

Умное удержание (Churn Prevention):
- Step 1 (24ч): персонализированный вопрос на основе темы гайда
- Step 2 (3 дня): практический кейс + AI-генерированный вопрос
- Step 3 (7 дней): предложение бесплатной мини-консультации

AI анализирует тему гайда и генерирует релевантный вопрос,
чтобы вовлечь пользователя в диалог.
"""

import logging

from aiogram import Bot
from aiogram.types import InlineKeyboardButton, InlineKeyboardMarkup

from src.bot.utils.cache import TTLCache
from src.bot.utils.google_sheets import GoogleSheetsClient

logger = logging.getLogger(__name__)

# Fallback-тексты если Google Sheets недоступен
FALLBACK_FOLLOWUP: dict[int, str] = {
    1: (
        "👋 Здравствуйте! Вчера вы скачали наш гайд.\n\n"
        "Удалось ли начать изучение? Если есть вопросы — "
        "мы всегда готовы помочь!\n\n"
        "📩 Для консультации напишите нам: @SOLISlegal"
    ),
    2: (
        "📊 Привет! Прошло несколько дней с момента скачивания гайда.\n\n"
        "Хотим поделиться практическим кейсом SOLIS Partners по этой теме. "
        "Подписывайтесь на наш канал, чтобы не пропустить полезные материалы!\n\n"
        "📚 Другие гайды: /start"
    ),
    3: (
        "🎯 Добрый день! Надеемся, гайд оказался полезным.\n\n"
        "Мы предлагаем <b>бесплатную мини-консультацию</b> (15 минут) "
        "по теме гайда с нашим специалистом.\n\n"
        "Для записи напишите нам: @SOLISlegal\n\n"
        "📚 Посмотреть другие гайды: /start"
    ),
}

# Маппинг guide_id -> тема для AI-персонализации
_GUIDE_TOPICS: dict[str, str] = {
    "too": "регистрация ТОО в Казахстане",
    "ip": "открытие ИП в Казахстане",
    "mfca": "регистрация компании в МФЦА (AIFC)",
    "esop": "опционные программы ESOP для стартапов",
    "taxes": "налогообложение в Казахстане",
    "labor": "трудовое право и найм сотрудников",
    "it_law": "IT-право и защита данных",
    "ma": "сделки M&A и корпоративное право",
}


def _personalized_fallback(step: int, name: str = "", sphere: str = "") -> str:
    """Fallback-текст с подстановкой имени и сферы."""
    greeting = f"{name}, здравствуйте" if name else "Здравствуйте"
    sphere_hint = f" для {sphere}-бизнеса" if sphere else ""

    templates = {
        1: (
            f"👋 {greeting}! Вчера вы скачали наш гайд.\n\n"
            "Удалось ли начать изучение? Если есть вопросы — "
            "мы всегда готовы помочь!\n\n"
            "📩 Для консультации напишите нам: @SOLISlegal"
        ),
        2: (
            f"📊 {greeting}! Прошло несколько дней с момента скачивания гайда.\n\n"
            f"Хотим поделиться практическим кейсом SOLIS Partners{sphere_hint}. "
            "Подписывайтесь на наш канал, чтобы не пропустить полезные материалы!\n\n"
            "📚 Другие гайды: /start"
        ),
        3: (
            f"🎯 {greeting}! Надеемся, гайд оказался полезным.\n\n"
            f"Мы предлагаем <b>бесплатную мини-консультацию</b> (15 минут){sphere_hint} "
            "с нашим специалистом.\n\n"
            "Для записи напишите нам: @SOLISlegal\n\n"
            "📚 Посмотреть другие гайды: /start"
        ),
    }
    return templates.get(step, templates.get(1, ""))


async def _generate_personalized_question(
    guide_id: str,
    step: int,
    *,
    name: str = "",
    sphere: str = "",
) -> str | None:
    """AI генерирует персонализированный вопрос на основе темы гайда и сферы.

    Returns:
        Персонализированный текст или None при ошибке.
    """
    topic = _GUIDE_TOPICS.get(guide_id, "")
    if not topic:
        topic = guide_id.replace("_", " ").replace("-", " ")

    # Контекст сферы для AI
    sphere_ctx = ""
    if sphere:
        sphere_ctx = f" Его сфера бизнеса: {sphere}."
    name_ctx = f" Имя пользователя: {name}." if name else ""

    try:
        from src.bot.utils.ai_client import ask_marketing

        step_instructions = {
            1: (
                f"Пользователь скачал гайд по теме «{topic}» 24 часа назад."
                f"{name_ctx}{sphere_ctx}\n"
                "Напиши КОРОТКОЕ (2-3 предложения) дружеское сообщение:\n"
                "1. Обратись по имени (если известно)\n"
                "2. Задай КОНКРЕТНЫЙ практический вопрос по теме гайда, "
                "привязанный к сфере бизнеса пользователя "
                "(например, для IT: 'Возникли ли вопросы по IT-льготам?')\n"
                "3. Предложи помощь от SOLIS Partners"
            ),
            2: (
                f"Пользователь скачал гайд по теме «{topic}» 3 дня назад, "
                f"но не прошёл консультацию.{name_ctx}{sphere_ctx}\n"
                "Напиши сообщение (3-4 предложения):\n"
                "1. Обратись по имени (если известно)\n"
                "2. Расскажи о РЕАЛЬНОМ кейсе по этой теме из сферы пользователя "
                "(придумай убедительный)\n"
                "3. Задай вопрос, который заставит задуматься о необходимости юриста\n"
                "4. Упомяни бесплатную AI-консультацию через /consult"
            ),
            3: (
                f"Пользователь скачал гайд по теме «{topic}» неделю назад, "
                f"не консультировался и может уйти.{name_ctx}{sphere_ctx}\n"
                "Напиши МОТИВИРУЮЩЕЕ сообщение (3-4 предложения):\n"
                "1. Обратись по имени (если известно)\n"
                "2. Предложи бесплатную 15-минутную мини-консультацию\n"
                "3. Объясни, чем это полезно конкретно для его сферы бизнеса\n"
                "4. Создай мягкую срочность ('на этой неделе')\n"
                "5. Дай ссылку на @SOLISlegal"
            ),
        }

        instruction = step_instructions.get(step, step_instructions[1])

        result = await ask_marketing(
            prompt=instruction,
            max_tokens=256,
            temperature=0.7,
        )
        return result.strip()

    except Exception as e:
        logger.warning("AI followup generation failed: %s", e)
        return None


async def send_followup_message(
    user_id: int,
    guide_id: str,
    step: int,
    *,
    bot: Bot,
    google: GoogleSheetsClient,
    cache: TTLCache,
) -> None:
    """Отправляет follow-up сообщение пользователю.

    Args:
        user_id: Telegram ID пользователя.
        guide_id: ID гайда, который скачал пользователь.
        step: Шаг серии (1, 2 или 3).
        bot: Экземпляр бота для отправки сообщений.
        google: Клиент Google Sheets.
        cache: TTL-кеш.
    """
    try:
        # 0. Подтягиваем имя и сферу пользователя для персонализации
        from src.database.crud import get_lead_by_user_id
        lead = await get_lead_by_user_id(user_id)
        lead_name = lead.name if lead else ""
        lead_sphere = getattr(lead, "business_sphere", "") or "" if lead else ""

        # 1. Проверяем: может пользователь уже прошёл консультацию?
        consult_log = await google.get_consult_log(limit=50)
        user_consulted = any(
            str(row.get("user_id", row.get("User ID", ""))) == str(user_id)
            for row in consult_log
        )

        if user_consulted and step == 1:
            greeting = f"👋 {lead_name}, р" if lead_name else "👋 Р"
            text = (
                f"{greeting}ады, что вы уже воспользовались нашей AI-консультацией!\n\n"
                "Если возникнут новые вопросы — пишите /consult, "
                "а для углублённой проработки обращайтесь к @SOLISlegal."
            )
            await bot.send_message(chat_id=user_id, text=text)
            logger.info("Follow-up (consulted user): user_id=%s, step=%d", user_id, step)
            return

        # 2. Пробуем AI-генерацию с учётом имени и сферы
        ai_text = await _generate_personalized_question(
            guide_id, step, name=lead_name, sphere=lead_sphere,
        )

        # 3. Если AI не справился — fallback из Sheets или стандартный
        if not ai_text:
            followup_texts = await cache.get_or_fetch(
                "followup_series",
                google.get_followup_series,
            )

            specific_key = f"{guide_id}_step_{step}"
            generic_key = f"step_{step}"

            ai_text = (
                followup_texts.get(specific_key)
                or followup_texts.get(generic_key)
                or _personalized_fallback(step, lead_name, lead_sphere)
            )

        if not ai_text:
            logger.warning("Текст follow-up не найден: step=%d, guide=%s", step, guide_id)
            return

        # 4. Добавляем кнопки для вовлечения
        buttons = [
            [InlineKeyboardButton(
                text="🤖 Задать вопрос AI-юристу",
                callback_data="start_consult",
            )],
            [InlineKeyboardButton(
                text="📚 Другие гайды",
                callback_data="show_all_guides",
            )],
        ]
        if step >= 2:
            buttons.insert(0, [InlineKeyboardButton(
                text="📞 Записаться на консультацию",
                url="https://t.me/SOLISlegal",
            )])

        keyboard = InlineKeyboardMarkup(inline_keyboard=buttons)

        try:
            await bot.send_message(
                chat_id=user_id,
                text=ai_text,
                reply_markup=keyboard,
            )
        except Exception:
            await bot.send_message(
                chat_id=user_id,
                text=ai_text,
                reply_markup=keyboard,
                parse_mode=None,
            )

        logger.info(
            "Follow-up отправлен: user_id=%s, guide=%s, step=%d (AI-personalized)",
            user_id, guide_id, step,
        )
    except Exception as e:
        logger.error(
            "Ошибка отправки follow-up: user_id=%s, step=%d, error=%s",
            user_id, step, e,
        )
